/**
 * Default case ID for development.
 * After visiting /debug and finding your case, paste its ID here.
 * Used as fallback when no caseId is in the URL.
 */
export const DEFAULT_CASE_ID = "PASTE_YOUR_CASE_ID_HERE"
