import { CasesDashboard } from "@/components/entryway/cases-dashboard"

/**
 * Dashboard — the Entryway view shown when no case is selected.
 */
export default function DashboardPage() {
  return <CasesDashboard />
}
